<%= form_for @task do |f| %>

	<%= f.label :content %>:
	<%= f.text_field :content, required: true %>

	<%= f.label :description %>:
	<%= f.text_field :description %>

	<%= f.label :priority %>:
	<%= f.number_field :priority, required: true %>

	<%= f.button :submit %>

<% end %>